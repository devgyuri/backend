describe('UsersResolver', () => {
  beforeAll(() => {
    // 로그인 등 공통 기능
  });

  describe('fetchUser', () => {
    it('fetch user data', () => {
      // 컨트롤러 테스트와 동일한 방식으로 진행
    });
  });

  describe('createUser', () => {
    it('create user data', () => {
      // 컨트롤러 테스트와 동일한 방식으로 진행
    });
  });
});
